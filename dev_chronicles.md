Okay, so this is the file I alter to keep track of every change planned, and what do I do in every commit.
Its purpose is, as you may infer, to 
a. help me keep accountability
b. learn how to write commits in a proper way

So here it goes:

#2026
##June
16: the start of this all. My plan is to develop a data collection part first (which dumps data into a CSV), then a map integration that can pull that CSV data and show it in the map itself. In addition, a folder called collection will hold an API which I can call from an endpoint.
24: the idea for a 'collection' development was dumb: will go with an SPA with a pop-up form once users want to add in data. Will re-launch it all in Vite+React to simplify headaches.